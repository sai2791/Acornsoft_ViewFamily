osbyte $&85  on entry X = for which mode
        Exit X and Y point to the first byte of screen RAM if mode X were chosen

L0008 seems to be something to do with mode or next mode to choose.

L004A and L004B seem to be how many bytes of memory are left for text
